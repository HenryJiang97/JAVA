// Definition
Set<T> set = new HashSet<>();

// Add
set.add(a);

// Remove
set.remove(a);

// Contains
set.contains(a);

// Empty
set.isEmpty();

// Iteration
Iterator<T> it = set.iterator();
while (it.hasNext()) {
    System.out.println(it.next());
}